# Mobile app environment variables
# All EXPO_PUBLIC_ variables are bundled into the app at build time

# API base URL
# Simulator/emulator: http://localhost:4501 (default)
# Physical device:    http://<your-machine-local-ip>:4501
EXPO_PUBLIC_API_URL=http://localhost:4501
